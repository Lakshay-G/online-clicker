1. If any problem regarding the browser or connection arise when running the python script is running, try updating the chromedriver by searching chromedriver selenium on google.

2. If you have any other browser, not chrome, download the web driver for that browser instead of the chromedriver. And also change the python script to match specific browser.

3. For Edge, download msedgedriver.
